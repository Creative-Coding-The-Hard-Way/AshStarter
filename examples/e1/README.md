# Example 1 - RenderPass + Framebuffer

This example shows the bare minimum Vulkan resources required to clear the
screen using a Vulkan RenderPass.

## Commands

From the project root: `cargo run --example e1`
